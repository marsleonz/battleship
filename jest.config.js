module.exports = {
  rootDir: "./", // Optional: set the root directory for your project
  testMatch: ["**/__tests__/**/*.js?(x)", "**/?(*.)+(spec|test).js?(x)"], // Optional: configure the test file pattern
};
